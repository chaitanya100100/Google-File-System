syntax = "proto3";

package master_server;

service MasterServerToClient {
    rpc GetFileList(Empty) returns (stream FilePath) {}
}

message FilePath {
    string file_path = 1;
}

message Empty {
}
